:TITLE[KernelOccupied];*Edited 1 May 1981 by Fiala for new Kernel.*Locations reserved on page 0 (This will all be overwritten by code in FAULT)**NOTE: Midas overlays send control to UserFault at absolute 120, so the**code in FAULT.MC must handle that analogously to Kernel.*IMReserve[0,0,2];*IMReserve[0,100,5];*IMReserve[0,106,13];*Locations reserved on page 15B (may be overwritten after Kernel is running)*IMReserve[15,0,50];*Locations reserved on page 16B (timer task)*This code may be overwritten after a user program starts its own timer task.*IMReserve[16,0,17];	*EverythingIMReserve[16,1,14];	*Only these are needed after Kernel is running*Locations reserved on page 17BIMReserve[17,0,1];	*7400 not needed after initIMReserve[17,3,1];	*7403 not needed after initIMReserve[17,1,2];IMReserve[17,4,74];IMReserve[17,120,1];IMReserve[17,100,20];	*space for Midas overlays (7500-7517):END;